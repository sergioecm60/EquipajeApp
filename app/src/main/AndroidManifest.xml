<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools">

    <uses-feature
        android:name="android.hardware.camera"
        android:required="true" />

    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.ACCESS_NETWORK_STATE" />
    <uses-permission android:name="android.permission.CAMERA" />
    <uses-permission android:name="android.permission.VIBRATE" />

    <application
        android:name=".EquipajeApplication"
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.EquipajeApp"
        android:networkSecurityConfig="@xml/network_security_config"
        tools:targetApi="34">

        <activity
            android:name=".ui.MainActivity"
            android:exported="true"
            android:windowSoftInputMode="adjustResize">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

        <activity
            android:name=".ui.login.LoginActivity"
            android:exported="false"
            android:theme="@style/Theme.EquipajeApp.NoActionBar" />

        <activity
            android:name=".ui.scanner.QrScannerActivity"
            android:exported="false"
            android:theme="@style/Theme.EquipajeApp.FullScreen" />

        <activity
            android:name=".ui.dashboard.DashboardActivity"
            android:exported="false" />

        <activity
            android:name=".ui.registro.RegistroEquipajeActivity"
            android:exported="false"
            android:label="Registro de Equipaje"
            android:theme="@style/Theme.EquipajeApp.NoActionBar" />

        <activity
            android:name=".ui.servicio.ServicioDetalleActivity"
            android:exported="false"
            android:label="Detalle del Servicio"
            android:parentActivityName=".ui.dashboard.DashboardActivity" />

    </application>

</manifest>
